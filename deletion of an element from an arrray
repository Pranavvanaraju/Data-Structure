#include<stdio.h>
int main()
{
    int a[10],i,n,pos;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    scanf("%d",&pos);
    for(i=pos;i<n-1;i++)
    {
        a[i]=a[i+1];
    }
    for(i=0;i<n;i++)
    {
        printf("%d\n",a[i]);
    }
}
